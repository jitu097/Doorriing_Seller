{
  "name": "bazarse-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
